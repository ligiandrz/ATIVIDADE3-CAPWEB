//11. Crie um array de números e mostre apenas os maiores que 10.
let numeros = [5, 12, 8, 20, 3, 15];
for (let i = 0; i < numeros.length; i++) {
    if (numeros[i] > 10) {
    console.log(numeros[i]);
}
}